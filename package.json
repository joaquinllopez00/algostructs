{
  "name": "algostructs",
  "version": "1.0.0",
  "description": "All of the data structures Javascript is missing + some basic algorithm implementations.",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "test": "jest",
    "format": "prettier --write \"src/**/*.ts\"",
    "prepare": "npm run build",
    "prepublishOnly": "npm test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
